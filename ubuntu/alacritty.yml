# Ref: https://github.com/alacritty/alacritty/releases/download/v0.11.0-rc2/alacritty.yml
#
window:
  #decorations: none
  opacity: 0.8
  # We require padding so that with a transparent window the prompt
  # doesn't start right up at the top.
  # padding:
  #   y: 27

dynamic_title: true

cursor:
  style: Block

font:
  size: 11.0
  #normal:
    #family: "JetBrains Mono"

key_bindings:
  - { key: K, mods: Command, chars: "clear\x0d" }
  - { key: V, mods: Command, action: Paste }
  - { key: C, mods: Command, action: Copy }
  - { key: Key0,           mods: Command,            action: ResetFontSize    }
  - { key: Equals,         mods: Command,            action: IncreaseFontSize }
  - { key: Plus,           mods: Command,            action: IncreaseFontSize }
  - { key: NumpadAdd,      mods: Command,            action: IncreaseFontSize }
  - { key: Minus,          mods: Command,            action: DecreaseFontSize }
  - { key: NumpadSubtract, mods: Command,            action: DecreaseFontSize }
  - { key: F,              mods: Command,            action: SearchForward    }
